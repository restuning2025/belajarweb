/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Fix for proper file resolution
  serverExternalPackages: [],
  // Add your other configurations here
}

module.exports = nextConfig
